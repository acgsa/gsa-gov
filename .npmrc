# cloud.gov Node.js buildpack: keep npm from injecting legacy flags
# (--unsafe-perm / --nodedir) that newer npm rejects during native rebuilds.
# See docs/decisions/ADR-007-cloudgov-sandbox-preview.md
unsafe-perm=true
legacy-peer-deps=true
